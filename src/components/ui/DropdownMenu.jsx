import { useEffect, useRef, useState } from 'react'
import { ChevronDown } from 'lucide-react'

export default function DropdownMenu({ label='Options',items=[] }) { const [open,setOpen]=useState(false); const ref=useRef(null); useEffect(()=>{const close=(e)=>{if(e.key==='Escape'||!ref.current?.contains(e.target))setOpen(false)};document.addEventListener('mousedown',close);document.addEventListener('keydown',close);return()=>{document.removeEventListener('mousedown',close);document.removeEventListener('keydown',close)}},[]); return <div ref={ref} className="relative"><button aria-expanded={open} className="btn-secondary" onClick={()=>setOpen(!open)}>{label}<ChevronDown size={14}/></button>{open&&<div role="menu" className="popover-enter absolute right-0 z-30 mt-1 min-w-44 rounded-lg border border-slate-200 bg-white p-1.5 shadow-float">{items.map((item)=><button role="menuitem" key={item.label} onClick={()=>{item.onClick?.();setOpen(false)}} className="block w-full rounded px-3 py-2 text-left text-xs hover:bg-slate-50">{item.label}</button>)}</div>}</div> }
